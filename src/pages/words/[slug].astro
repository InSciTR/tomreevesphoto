---
import { getEntry } from 'astro:content';

const { slug } = Astro.params;
const post = await getEntry('words', slug);
if (!post) throw new Error(`Post not found: ${slug}`);

const { Content, data } = post;
---

<Content />